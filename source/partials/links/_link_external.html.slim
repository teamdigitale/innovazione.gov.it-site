- html_class ||= "btn btn-primary mr-2"
- label ||= cta_label(item)

= proxy_link_to label,
  item.link,
  class: html_class,
  target: "_blank",
  rel: "noopener",
  "aria-label": "#{label}: (Link esterno) #{item.title}"
