- page.content_blocks.each do |block|
  div id="#{block.respond_to?(:text_title) ? block.text_title.downcase.gsub(' ', '-') : ''}" = partial "partials/blocks/#{block.item_type.api_key}",
    locals: {block: block}