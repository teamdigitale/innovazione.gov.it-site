= javascript_include_tag  "application"

script type="text/javascript" src="https://unpkg.com/datocms-search@0.1.8/dist/datocms-search.widget.js"

= yield_content :page_js

javascript:
  const searchClient = new DatoCmsSearch(
    "#{data.config.search_token}",
    "production"
  );

  const initSearch = () => {
    if (searchClient === null) {
      return null;
    }
    return searchClient.addWidget("#search-container", {});
  };

  initSearch();

- if build?
  javascript:
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', '#{data.config.google_analytics_key}', 'auto');
      ga('set', 'anonymizeIP', true);
      ga('send', 'pageview');
